{
  "name": "restapi",
  "version": "1.0.0",
  "description": "a simple api that takes array in and return value",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  "author": "SumelRattan",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^4.17.3"
  }
}
